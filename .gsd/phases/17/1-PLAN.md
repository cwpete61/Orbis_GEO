---
phase: 17
plan: 1
wave: 1
---

# Phase 17 - Wave 1: Simulation Data Schema

## <task> Implement Data Schema for Simulation Results
1. Create `schema/sim_schema.json` to define the structured output of `sim_engine.py`.
2. Connect the `generate_live_pdf.py` logic to load this schema (or a similar data structure output by the sim engine) so it can extract the metrics.

## <verify>
- Ensure `schema/sim_schema.json` is created and follows the structure generated by Phase 16.
